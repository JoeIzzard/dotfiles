# Add Bin to Path
export PATH="$HOME/.dotfiles/bin:$PATH"

# Load the Aliases System
source $HOME/.dotfiles/Aliases/load.sh

# Load the extras file if it exists
if [ -e $HOME/.dotfiles/.extras ]; then
    source $HOME/.dotfiles/.extras
fi