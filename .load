# Add Bin to Path
export PATH=$~/.dotfiles/bin:$PATH

# Load the Aliases System
source ~/.dotfiles/Aliases/load.sh

# Load the extras file if it exists
if [ -e ~/.dotfiles/.extras ]; then
    source ~/.dotfiles/.extras
fi